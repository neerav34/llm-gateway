# Secrets — never commit API keys
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Local data (cost tracking DB comes in Day 4)
*.db

# Internal-doc plaintext source + builder — never commit these.
# Only the encrypted output (public/internal.html) is meant to be public;
# committing the plaintext would make the passphrase decorative.
scripts/internal_doc/
